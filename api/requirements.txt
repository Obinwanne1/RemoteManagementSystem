flask==3.1.3
flask-sqlalchemy==3.1.1
flask-migrate==4.1.0
flask-jwt-extended==4.7.1
flask-cors==6.0.5
flask-limiter==4.1.1
flask-compress==1.15
marshmallow==3.26.2
celery==5.5.2
redis==5.2.1
psycopg2-binary==2.9.12
bcrypt==5.0.0
pyotp==2.9.0
python-dotenv==1.2.2
gunicorn==23.0.0
reportlab==4.4.1
openpyxl==3.1.5
python-dateutil==2.9.0
requests==2.33.0
Pillow==12.3.0
stripe==12.3.0
anthropic>=0.40.0,<2.0.0
# audits/security_audit.md Finding D2 — every dependency below now has an
# upper bound (capped at the next major above what's currently installed and
# tested), matching the convention paho-mqtt/pysnmp below already use, so a
# future install can't silently pull in an untested major version.
cachetools>=5.0.0,<8.0.0
waitress>=3.0.0,<4.0.0
sentry-sdk[flask]>=2.0.0,<3.0.0
cryptography>=42.0.0,<51.0.0
# tasks/mqtt_tasks.py uses the 1.x callback signatures (on_connect(client, userdata,
# flags, rc), mqtt.Client() with no CallbackAPIVersion) — paho-mqtt 2.0 changed both.
paho-mqtt>=1.6.1,<2.0.0
# tasks/snmp_tasks.py imports the classic synchronous pysnmp.hlapi API
# (getCmd/SnmpEngine/CommunityData/...). That module was removed starting with
# pysnmp 5.x (lextudio fork, asyncio-first rewrite) — confirmed by import error
# against the installed 7.1.29. pyasn1 is pinned alongside it: a fresh pysnmp==4.4.12
# install otherwise pulls latest pyasn1 (0.6.x), which dropped the pyasn1.compat
# module pysnmp 4.4.x's hlapi imports at load time.
pysnmp>=4.4.12,<5.0.0
# SECURITY NOTE (audits/security_audit.md Finding D1): pyasn1 0.4.8 has 3 known
# advisories (PYSEC-2026-2263, -3456, -3457) fixed in 0.6.3/0.6.4 — NOT upgraded
# here. Confirmed by direct test: installing pyasn1==0.6.4 alongside pysnmp==4.4.12
# breaks `import pysnmp.hlapi` outright (ModuleNotFoundError: pyasn1.compat.octets
# no longer exists), exactly as the comment above already predicted. Fixing this
# requires rewriting tasks/snmp_tasks.py onto pysnmp 5.x/7.x's async API (a real
# subsystem rewrite, not a version bump) — out of scope for this pass. Risk is
# narrow (SNMP response *parsing* only, from devices already on the polled
# customer's own network) but real; tracked here rather than silently ignored.
pyasn1<0.5.0
google-auth>=2.30.0,<3.0.0
