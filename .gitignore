# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg

# Virtual environments
venv/
env/
.venv/
api/venv/
agent/venv/
dashboard/venv/

# Environment
.env
*.env

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
logs/

# Locust load-test run output (scratch — see tests/load/locustfile.py)
.claude/loadtest_*.csv

# Database
*.db
*.sqlite3

# Redis dump
dump.rdb
api/dump.rdb

# Vendored Redis binaries (install Redis/Memurai separately; don't commit it)
redis/
redis.zip

# One-off scratch/patch scripts (not part of the maintained toolchain)
chunk.txt
debug.txt
patch_handover*.py
patch_technical*.py
generate_pdf.py

# Reports / uploads
reports/output/
api/reports/
uploads/
scripts_library/custom/

# Agent screenshots (runtime capture, may show a real device's screen)
api/screenshots/

# Celery beat schedule (runtime artifacts — filenames vary by Python's dbm backend:
# sqlite3 on 3.13+ produces -shm/-wal, dbm.dumb on 3.11 produces .bak/.dat/.dir)
api/celerybeat-schedule
api/celerybeat-schedule-shm
api/celerybeat-schedule-wal
api/celerybeat-schedule.bak
api/celerybeat-schedule.dat
api/celerybeat-schedule.dir

# PyInstaller
agent/dist/
agent/build/
agent/*.spec

# Streamlit
.streamlit/secrets.toml

# Agent runtime config — contains a live org token / device_id / encrypted agent_token
agent/config.ini

# Coverage
.coverage
htmlcov/
.pytest_cache/

# Alembic
api/migrations/versions/*.pyc

# TLS certificates (never commit private keys)
nginx/certs/

# MDM credentials (defense-in-depth — design stores these encrypted in the DB
# via upload, never intentionally written to disk, but guard against it anyway)
*.p12
*.pem
google-service-account*.json

# Nightly pg_dump output (backup_tasks.py default BACKUP_DIR) — raw DB dump, never commit
backups/
