# API base URL
# Leave as /api when served behind nginx or Vite dev proxy (default)
# Override with full URL for standalone deployments without a reverse proxy:
#   VITE_API_URL=https://your-domain.com/api
VITE_API_URL=/api
